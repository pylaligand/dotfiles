[alias]
    lo = "log --oneline --decorate"
    nb = "!git checkout -b $1 origin/main #"
    po = "push origin HEAD"
    pof = "push --force origin HEAD"

[core]
    excludesFile = ~/.my.gitignore

[rebase]
    autoStash = true
